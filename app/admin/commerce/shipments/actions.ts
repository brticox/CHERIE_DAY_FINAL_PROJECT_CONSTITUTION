'use server';import{redirect}from'next/navigation';import{revalidatePath}from'next/cache';import{requireStaff}from '@/lib/auth/guards';import{can}from '@/lib/admin/permissions';import{createAdminClient}from '@/lib/supabase/admin';
const transitions:Record<string,string[]>={preparing:['shipped'],shipped:['in_transit'],in_transit:['delivered','returned'],delivered:[],returned:[]};
export async function transitionShipment(formData:FormData){const{staff}=await requireStaff('/admin/commerce/shipments');if(!can(staff.role,'orders.transition'))redirect('/admin/commerce/shipments?error=permission');const id=String(formData.get('id'));const next=String(formData.get('status')) as 'preparing'|'shipped'|'in_transit'|'delivered'|'returned';const db=createAdminClient();const{data:row}=await db.from('shipments').select('status,order_id').eq('id',id).single();if(!row||!transitions[row.status]?.includes(next))redirect('/admin/commerce/shipments?error=transition');const now=new Date().toISOString();const patch:{status:typeof next;shipped_at?:string;delivered_at?:string}={status:next};if(next==='shipped')patch.shipped_at=now;if(next==='delivered')patch.delivered_at=now;const{error}=await db.from('shipments').update(patch).eq('id',id);if(error)redirect('/admin/commerce/shipments?error=save');await db.from('tracking_events').insert({shipment_id:id,status:next,message_tr:`Yönetim durumu: ${next}`,occurred_at:now});await db.from('audit_log').insert({staff_user_id:staff.id,action:'shipment.transitioned',entity_type:'shipment',entity_id:id,diff:{from:row.status,to:next,order_id:row.order_id}});revalidatePath('/admin/commerce/shipments')}
