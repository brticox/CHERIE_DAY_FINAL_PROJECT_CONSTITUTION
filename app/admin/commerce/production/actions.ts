'use server';import{redirect}from'next/navigation';import{revalidatePath}from'next/cache';import{requireStaff}from '@/lib/auth/guards';import{can}from '@/lib/admin/permissions';import{createAdminClient}from '@/lib/supabase/admin';
const transitions:Record<string,string[]>={blocked:['ready'],ready:['in_production','blocked'],in_production:['quality_check','blocked'],quality_check:['passed','rework'],rework:['in_production'],passed:['packed'],packed:['completed'],completed:[]};
export async function transitionProduction(formData:FormData){const{staff}=await requireStaff('/admin/commerce/production');if(!can(staff.role,'orders.transition'))redirect('/admin/commerce/production?error=permission');const id=String(formData.get('id'));const next=String(formData.get('status'));const db=createAdminClient();const{data:row}=await db.from('production_jobs').select('status').eq('id',id).single();if(!row||!transitions[row.status]?.includes(next))redirect('/admin/commerce/production?error=transition');const patch:{status:string;started_at?:string;completed_at?:string}={status:next};if(next==='in_production')patch.started_at=new Date().toISOString();if(next==='completed')patch.completed_at=new Date().toISOString();const{error}=await db.from('production_jobs').update(patch).eq('id',id);if(error)redirect('/admin/commerce/production?error=save');await db.from('audit_log').insert({staff_user_id:staff.id,action:'production.transitioned',entity_type:'production_job',entity_id:id,diff:{from:row.status,to:next}});revalidatePath('/admin/commerce/production')}
