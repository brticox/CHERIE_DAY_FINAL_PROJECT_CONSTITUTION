# CHERIE DAY — Apple Hosted Readiness

Apple Developer tooling is not connected. `AUTH_APPLE_ENABLED` remains false in all environments.

Owner actions: create a Services ID, verify `cherieday.eu`, register the exact project-specific Supabase callback, securely store Team ID/Key ID/private key outside source control, establish six-month secret rotation, and register the Resend sender with Apple Private Email Relay when required. Staging-only proof is required before any production proposal.
